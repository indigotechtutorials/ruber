<div class="flex flex-col items-center pt-16">
  <h1 class="font-bold text-4xl">Viewing all pending Rides </h1>
  <div class="grid gap-4 mt-8">
    <% @available_rides.each do |ride| %>
      <%= render "available_ride", ride: ride %>
    <% end %>
  </div>
</div>
